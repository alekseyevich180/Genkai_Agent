# External tests

This tier is opt-in with `pytest -m external`. It is reserved for tests that
need a network service, VASP, GPU/CUDA, PJM, or an installed MLIP runtime.
It is intentionally empty in the offline validation suite.
