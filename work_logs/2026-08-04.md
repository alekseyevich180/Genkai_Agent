# 2026-08-04 工作日志

## 目标与范围

在 `Genkai_Evolution/` 专用 worktree 中继续方案 A 的结构收敛，完成 Task 12
第一个可独立验收的 PToModel 纵向切片：把稳定映射、modeling checklist 和
canonical surface-task schema 迁入 `src/genkai/modeling/`，清除最后两条
`src/genkai -> paperread` 反向 import，并保持 PToModel skill 为按需薄入口。

本切片不迁移依赖 ASE、pymatgen、Optuna 或 FAIRChem 的表面结构生成算法；这些
算法仍属于 Task 12 的下一切片。本轮也不处理既有 `agent.tools` 全量测试收集
阻断，不运行外部科研任务或推送远端。

## 设计与实施计划

- `docs/superpowers/specs/2026-08-04-genkai-ptomodel-convergence-design.md`
- `docs/superpowers/plans/2026-08-04-genkai-ptomodel-convergence.md`

选择分阶段纵向迁移，而不是一次移动约 3,600 行结构运行时代码。为避免把原
1,294 行纯映射与 artifact facade 强行合并，最终内部边界为：

- `src/genkai/modeling/mapping.py`：纯 PToModel 映射和 JSON 生成；
- `src/genkai/modeling/ptomodel.py`：稳定公共 API、CLI 与 artifact facade；
- `src/genkai/modeling/checklist.py`：checklist 和 compact bundle；
- `src/genkai/modeling/schema/task_parameter_schema.json`：canonical schema。

## 完成内容

- 将旧 `paperread/surface/modeling/ptomodel.py` 迁为
  `src/genkai/modeling/mapping.py`，保持任务选择、参数绑定和 JSON 结构规则。
- 将旧 `job_bundle.py` 迁为 `src/genkai/modeling/checklist.py`。
- 将 surface-task schema 从 Skill 私有目录迁入 Genkai package resource，改用
  `importlib.resources` 加载，并对版本、非空 task registry 做显式校验。
- `genkai.modeling.ptomodel` 公开 mapping、JSON generation、CLI 和
  artifact-aware `build_modeling_plan`；artifact producer 更新为当前所有者。
- PToModel skill wrapper 改为调用 `genkai.modeling.ptomodel.main`，没有复制映射
  或 checklist 规则。
- 删除 `paperread/surface/modeling/` 和 Skill 内的 schema 副本。
- 架构 gate 从两条精确 allowlist 收紧为零；wheel 检查确保 canonical schema
  在源码和安装包中逐字节一致。
- README、正式演进计划、结构基线与两个 Skill 文档更新为当前所有权和剩余边界。

## TDD 记录

1. 新 owner import 和 package-resource 测试先失败：
   `genkai.modeling.ptomodel` 尚未导出 mapping API，且 canonical resource 不存在。
   实现迁移后，mapping、surface literature 与 facade 回归为
   `29 passed, 7 subtests passed`。
2. checklist owner 和真实 Skill subprocess 测试先失败：
   `genkai.modeling.checklist` 尚不存在。迁移和薄入口改造后为
   `9 passed, 10 warnings`。
3. architecture gate 先通过，但 wheel resource 测试按预期失败，指出安装包缺少
   `genkai/modeling/schema/task_parameter_schema.json`。声明 package data 后，
   gate 与 wheel 为 `5 passed, 4 warnings`。

## 最终验证

所有命令均使用共享 `../.venv` 并在当前 worktree 前台执行。

相关完整回归：

```bash
../.venv/bin/python -m pytest \
  tests/architecture tests/modeling \
  tests/integrations/test_surface_facades.py \
  tests/workflow/test_surface_paper.py \
  tests/workflow/test_paper_to_mlip.py \
  tests/skills/test_ptomodel_entrypoint.py \
  tests/packaging/test_wheel_contents.py \
  tests/test_paperread_surface.py \
  tests/test_surface_mp_workflow.py -q --tb=short
```

代码审查修正后的最终结果：
`50 passed, 14 warnings, 7 subtests passed in 27.25s`。警告来自既有
ADK `BaseAgentConfig`、ASE/NumPy 和 spglib deprecation。

完成扫描确认：

- `src/` 中不存在 `paperread.surface.modeling` 或
  `agents.Agent.skills` import；
- 当前 README、正式计划、pyproject 和 Skill 文档不再引用旧 schema 路径；
- `paperread/surface/modeling/` 物理路径不存在；
- `git diff --check` 通过。

独立代码审查没有发现 Critical 问题。审查指出当前映射文档和正式恢复顺序仍有
已删除 owner 的活动引用，已改为 `genkai.modeling` 当前路径；同时将 wheel
测试加强为从 extracted wheel 的 `PYTHONPATH` 通过 `importlib.resources`
实际加载 schema，并确认导入模块来自 wheel，而不只比较归档字节。

仓库级收集检查：

```bash
../.venv/bin/python -m pytest -q --tb=short
```

结果：在运行测试前因 `tests/test_structure_builder.py` 导入不存在的
`agent.tools.structure_builder` 而停止，`1 error, 54 warnings`。这是前序日志已
记录且本轮明确排除的既有边界，因此不能声称仓库全量测试通过。

## 提交检查点

- `9c232cd` `docs: design ptomodel structure convergence`
- `e94cb6d` `docs: clarify ptomodel module boundary`
- `acc6fb5` `docs: plan ptomodel structure convergence`
- `5da5e64` `refactor: move ptomodel mapping into genkai`
- `efc6222` `refactor: make ptomodel skill a thin genkai entry`
- `8d8b5d7` `test: close ptomodel ownership boundary`
- `ed25a49` `fix: align ptomodel handoff and wheel verification`

## 明确未执行

本轮没有运行在线 LLM 抽取、VASP、GPU/CUDA、PJM、MACE/UMA 科学推理、
DeepMD 训练、UMA 微调、结构弛豫、分子动力学或远端 Git push。离线 fixture、
CLI 和 wheel 验证不构成真实科研计算证据。

## 后续边界

Task 12 尚未整体完成。下一切片应单独设计 surface-modeling 算法所有权，按依赖
和运行时拆分 vacancy、adsorbate、metal-cluster、Materials Project slab 与
FAIRChem search；在测试覆盖不足时不得一次性搬迁全部脚本。Task 13–15 保持未
开始状态。

## Task 12 surface-modeling 收口

本轮按独立设计与实施计划完成剩余 Task 12 算法迁移：vacancy、adsorbate、
Materials Project slab、metal-cluster 和 cluster-search 的生产实现归属
`src/genkai/modeling/surface/`；原 Skill 脚本路径仅保留 re-export/CLI 薄入口。
内部导入改为包相对路径，canonical schema 的四个 script 字段改为 Genkai
模块入口。未运行 Materials Project 在线访问、FAIRChem、GPU/PJM、VASP、训练、
弛豫或 MD。

离线验证：surface ownership/architecture `8 passed`；纸面 surface 与 MP
workflow `33 passed, 10 warnings`。完整 Task 12 回归和 wheel 检查随后执行；
仓库全量 pytest 仍受既有 `agent.tools.structure_builder` 缺失导入阻断，未将其
误报为本轮回归失败。

## Task 13 compute/dataset/MLIP 收口

Task 13 在既有 `src/genkai/compute`、`src/genkai/datasets` 和
`src/genkai/mlip` 实现基础上完成所有权收口：新增无科学运行时依赖的
`genkai.mlip.launchers` contract registry，MACE/DeepMD/UMA adapter 统一从
注册表获取 launcher 环境变量和协议标记；架构门禁确认 Skill 脚本不复制
dataset、artifact 或 training gate，UMA 数据验证继续调用 canonical ASE audit。

验证结果：Task13 focused suite `32 passed, 32 warnings`；VASP `--help` 成功；
MACE/DeepMD/UMA shell `bash -n` 成功；wheel 构建与 extracted-wheel launcher
导入检查成功。未运行 VASP、GPU/CUDA、PJM、MACE/DeepMD/UMA 推理或训练、
FAIRChem、在线服务。全量 pytest 的既有 `agent.tools.structure_builder` 收集
阻断仍单独记录，未归因于本轮。

## Task 14 test/fixture 收口

完成测试分层与 fixture 隔离：新增 `unit`、`contract`、`integration`、
`compatibility`、`external` pytest marker；通过 `tests/conftest.py` 按目录自动
标注测试层，并默认排除 external。原根目录的 paper/surface compatibility
characterization tests 已迁入 `tests/compatibility/`，保持原断言、mock 和旧
Skill 路径行为不变。

大型论文 PDF 与生成 experience/full-run 快照已迁入
`tests/fixtures/archives/{papers,generated}/`，并在 `tests/fixtures/README.md`
记录来源、用途与“pytest 不下载/不重生成”策略；`tests/fixtures/paths.py` 提供
稳定路径辅助函数。新增 `tests/external/` 作为外部运行时保留层，当前为空。

验证：离线分层套件 `101 passed, 42 warnings, 7 subtests passed`；compatibility
子集 `36 passed, 10 warnings, 7 subtests passed`；`tests/external --collect-only`
无测试收集。仓库全量收集仍受既有 `agent.tools.structure_builder` 缺失导入阻断，
未归因于 Task14。

## Task 15 根目录与弃用清理

完成旧 owner 与生成状态审查：`paperread/surface` 的源码已不属于当前 Git
工作树；保留 `paperread/NERRE` 和 `paperread/ReactionSeek` 等仍有用途的研究
资产，不做未经兼容验证的删除。清理本地忽略的 `build/` 与 `Genkai.egg-info/`
后，从干净源码重建 wheel，确认不再打包 `paperread/surface/`；新增
`docs/migration.md` 记录 owner、兼容入口和弃用边界。

新增 wheel 断言、更新 README/结构基线/映射文档和正式计划。Task15 验证仅使用
离线构建和既有兼容测试，不运行外部科研任务、不推送远端。

## 方案 A 物理结构收敛补充

根据主指南 `GENKAI_EVOLUTION_PLAN.md` 的方案 A，在隔离 worktree
`feat/genkai-evolution` 中继续执行物理结构优化，主工作区保持不变。审计确认
`src/genkai/` 已承担活动库所有权，而顶层 `paperread/` 仅剩 NERRE 与
ReactionSeek 历史研究资产；它们已整体迁入 `legacy/paperread/`，并新增
`legacy/README.md` 说明其仅作归档、不作为活动 package owner。

同步移除 setuptools 的 `paperread*` package discovery，增强 wheel 门禁以拒绝
任何 `paperread/` 文件；清理忽略的旧 `build/`/`Genkai.egg-info/` 后的干净 wheel
仅包含活动 Genkai/Agent 包。新增物理布局测试和方案 A 设计/实施计划；未移动仍
有活动职责的 `agents/Agent/skills/`、`start/`、`web/`。

验证结果：物理布局、architecture、packaging、compatibility、contracts、
modeling、literature、MLIP、integration、workflow 分层套件
`103 passed, 42 warnings, 7 subtests passed`；未运行网络、VASP、GPU/PJM、MLIP
推理/训练或其他外部科研任务。主工作区未修改。

补充收口：顶层 `paperread/README.md` 与 `paperread/requirements.txt` 也已归档
到 `legacy/paperread/`，并清除 worktree 中残留的 ignored `paperread/` 缓存目录；
活动源码根目录不再保留 `paperread/` 路径。
