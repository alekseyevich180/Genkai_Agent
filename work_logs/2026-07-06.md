# 2026-07-06 工作日志

## 新建并扩展 lobster 计算 skill

本日参考 `agents/Agent/skills/` 下现有计算类 skills 的组织方式，新建并扩展了一个 `lobster` skill，用于承接 LOBSTER 键合分析相关工作流。

新增目录：

```text
agents/Agent/skills/lobster/
  SKILL.md
  config.yaml
  scripts/
    lobster_tools.py
  references/
    vasp.md
    cp2k.md
    quantum-espresso.md
```

主要目标是把 LOBSTER 从“零散脚本经验”整理成与现有 Agent skill 体系一致的能力入口。

## 先建立 VASP -> LOBSTER 的主工作流

最初先按现有 `vasp`、`lammps`、`ptomodel` 等 skill 的写法，建立了 `lobster` 的基础能力：

- 在 `SKILL.md` 中定义 workflow、依赖关系、命令入口
- 在 `lobster_tools.py` 中实现：
  - `prepare_input`
  - `write_basis_template`
  - `read_results`
  - `collect_results`

其中：

- `prepare_input`
  - 从已完成的 VASP SCF 目录中收集：
    - `POSCAR`
    - `POTCAR`
    - `INCAR`
    - `KPOINTS`
    - `WAVECAR`
    - `CHGCAR`
  - 生成新的 LOBSTER 计算目录
  - 写入最小化、显式的 `lobsterin`

- `read_results`
  - 汇总 `lobsterout` 中的 spilling 信息
  - 读取 `ICOHPLIST.lobster`
  - 返回最强成键对、键数等摘要

## 将用户提供的 VASP LOBSTER 输入经验写入 skill

随后读取并参考了两个本地输入来源：

- `/home/pj24001724/ku40000345/wu/OER/Oneatom/graphene/angle/wu_icohp.sh`
- `/home/pj24001724/ku40000345/wu/OER/Uchimoto-IrO2/PBE_p42mnm_110_100/cohp/110/2O-Nband345/INCAR`

据此把 VASP 侧的 LOBSTER 计算风格正式写入 `lobster` skill，重点包括：

- `ISPIN = 1`
- `ENCUT = 400-500`
- `ALGO = Normal`
- `EDIFF = 1E-5`
- `NELM = 300-1000`
- `ISMEAR = 1` 或 `-5`
- `SIGMA = 0.05`
- `IBRION = -1`
- `NSW = 0`
- `ISIF = 2`
- `NPAR = 5-10`
- `LREAL = Auto`
- `NSIM = 1`
- `LPLANE = .TRUE.`
- `IVDW = 12`
- `LWAVE = .TRUE.`
- `LCHARG = .FALSE.` 或按下游需求切换为 `.TRUE.`
- `LORBIT = 12`
- `ISYM = -1`
- 显式 `NBANDS`

新增命令：

- `write_vasp_incar`
  - 用于直接生成符合本地 LOBSTER 工作流风格的 `INCAR` 模板

同时补充参考文档：

- `agents/Agent/skills/lobster/references/vasp.md`

## 补全 CP2K 和 Quantum ESPRESSO 两条路径

在补充 “其余软件中的 LOBSTER 使用方式” 时，没有停留在纯文档说明，而是继续把两条路径做成可执行的 skill 命令。

新增命令：

- `prepare_input_cp2k`
- `prepare_input_qe`
- `write_file_manifest`

设计上采用了“显式 file manifest staging”策略，而不是在脚本里硬编码 CP2K/QE 的接口文件名。原因是：

- 当前仓库中没有现成的 `cp2k` 或 `qe` skill 作为约束来源
- 本地依赖与 `pymatgen` 现状并未提供明确、稳定的 CP2K/QE LOBSTER staging 接口
- 不同环境下 CP2K 和 QE 的导出文件名、目录结构可能不同

因此当前实现方式是：

1. 用户先准备好 CP2K 或 QE 侧实际产出的 LOBSTER 所需文件
2. 用 `write_file_manifest --code cp2k|qe` 生成模板
3. 按本机真实文件名修改 manifest
4. 调用：
   - `prepare_input_cp2k`
   - `prepare_input_qe`

这些命令会：

- 从 `--source_dir` 按 manifest 显式收集文件
- 根据 `species` + `basis-functions` 生成 `lobsterin`
- 组装成独立的 LOBSTER 计算目录

对应新增参考文件：

- `agents/Agent/skills/lobster/references/cp2k.md`
- `agents/Agent/skills/lobster/references/quantum-espresso.md`

## 调整 lobster skill 文档定位

更新 `agents/Agent/skills/lobster/SKILL.md`：

- frontmatter `description` 从仅覆盖 VASP 改为覆盖：
  - VASP
  - CP2K
  - Quantum ESPRESSO
- 明确三条 staging 路径都通过 `lobster_tools.py`
- 增加命令说明：
  - `write_file_manifest`
  - `write_vasp_incar`
  - `prepare_input_cp2k`
  - `prepare_input_qe`
- 将不同软件的说明拆到 `references/`，保持 `SKILL.md` 主体简洁

## 校验

完成以下本地校验：

```bash
python -m py_compile agents/Agent/skills/lobster/scripts/lobster_tools.py
python agents/Agent/skills/lobster/scripts/lobster_tools.py --help
python agents/Agent/skills/lobster/scripts/lobster_tools.py write_vasp_incar --help
python agents/Agent/skills/lobster/scripts/lobster_tools.py write_file_manifest --help
python agents/Agent/skills/lobster/scripts/lobster_tools.py prepare_input_cp2k --help
python agents/Agent/skills/lobster/scripts/lobster_tools.py prepare_input_qe --help
```

结果：

- `lobster_tools.py` 语法检查通过
- VASP / CP2K / QE 新增命令均可正常输出帮助信息

## 当前实现边界

目前 `lobster` skill 的状态可以概括为：

- VASP 路径：
  - 已有较完整的可执行 staging 支持
  - 已结合本地项目实际 `INCAR` 风格

- CP2K / QE 路径：
  - 已有可执行 staging 命令
  - 但依赖用户显式提供文件清单，而不是脚本自动猜测接口文件

这种边界是有意保持的，目的是先保证 skill 可用且不误导，再根据后续真实样例进一步收紧校验规则。

## 下一步

1. 如果后续拿到稳定的 CP2K -> LOBSTER 或 QE -> LOBSTER 实际目录样例，可以把当前 manifest 模式升级为更强约束的 preset 校验。
2. 若仓库后续新增 `cp2k` 或 `qe` 相关 skill，可继续把其输入准备与 `lobster` 直接打通。
3. 可进一步补充：
   - 常见元素 `basisfunctions` 示例
   - Bohrium / Slurm 的 LOBSTER 提交模板
   - 更细的 `lobsterout` / `COHPCAR.lobster` 结果解析
