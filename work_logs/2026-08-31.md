# 2026-08-31 Genkai Harness Engineering 第一阶段落地

## 目标

将通用 Harness Engineering 方法落成适合当前 Genkai_Evolution 仓库的项目规则、
使用指南、自动检查和 CI 最低门禁。此次只修改隔离工作树，不修改旁边的主工作区，
不启动网络、LLM、Materials Project、VASP、MLIP、GPU/PJM、训练或 MD。

## 实际完成

- 新增根目录 `AGENTS.md`：记录项目地图、依赖方向、科学证据边界、安装命令、测试分层、
  修改规则、失败处理、工作日志约定和完成标准。
- 新增 `docs/harness-engineering.md`：提供面向 Genkai 任务的 Harness 使用方法、任务模板、
  分级验证、架构迁移和科学工作流示例、恢复流程与健康检查。
- 新增 `scripts/check_harness.py`，提供五个 profile：
  - `doctor`：Python、路径、pyproject、所有者、文档和 CI 静态契约；
  - `quick`：测试分层与架构测试；
  - `ci`：Agent 导入、架构测试与三个公共 CLI 帮助；
  - `package`：wheel 内容和解压导入契约；
  - `full`：完整默认测试，并如实暴露现有收集阻塞。
- 修改 `.github/workflows/test.yml`：CI 安装项目和 pytest 后运行
  `python scripts/check_harness.py ci`。
- 修改 `README.md`：增加 Harness 入口和常用命令，更新 CI 行为说明。
- 删除 `docs/superpowers/` 下 12 个历史 superpower specs/plans，并更新活动演进计划中的
  失效引用；历史日期日志仍保留当时的事实记录。
- 更新 `work_log.md` 的日期索引。

## 实际验证

当前工作树相邻的 `../.venv` 虽然配置文件记录 Python 3.12.6，但实际执行为 Python
3.6.8 且没有 pytest。本次没有修改该环境；只读借用了主仓库已有的 Python 3.12.9
环境，并通过脚本自动设置当前工作树的 `PYTHONPATH=src:.`。

执行：

```bash
/home/pj24001724/ku40000345/wu/Genkai_Agent/.venv/bin/python \
  scripts/check_harness.py doctor
```

结果：五项静态契约全部通过。

执行：

```bash
/home/pj24001724/ku40000345/wu/Genkai_Agent/.venv/bin/python \
  scripts/check_harness.py quick
```

结果：`12 passed in 0.34s`。

执行：

```bash
/home/pj24001724/ku40000345/wu/Genkai_Agent/.venv/bin/python \
  scripts/check_harness.py ci
```

结果：`21 passed in 45.44s`；`genkai.cli`、`genkai.cli surface` 和
`agent.init.start_agent` 的 `--help` 全部通过。

执行：

```bash
/home/pj24001724/ku40000345/wu/Genkai_Agent/.venv/bin/python \
  scripts/check_harness.py package
```

结果：`1 passed, 4 warnings in 24.09s`；wheel 内容契约通过。

执行：

```bash
/home/pj24001724/ku40000345/wu/Genkai_Agent/.venv/bin/python \
  scripts/check_harness.py full
```

结果：失败于测试收集，`54 warnings, 1 error in 69.91s`。错误仍是已知的
`tests/test_structure_builder.py` 导入不存在的
`agent.tools.structure_builder.structure_builder`，未发现由本次 Harness 文件引入的
新收集错误。

## 后续虚拟环境安装

在当前 `Genkai_Evolution` 工作区新建 `.venv`，明确使用可用的 CPython 3.12.9，
没有修改旁边主工作区的环境。由于默认 uv cache 路径只读，本次将安装缓存临时指向
`/tmp/genkai_evolution_uv_cache`。

执行：

```bash
UV_CACHE_DIR=/tmp/genkai_evolution_uv_cache uv venv .venv \
  --python /home/pj24001724/ku40000345/.rye/py/cpython@3.12.9/bin/python3.12
UV_CACHE_DIR=/tmp/genkai_evolution_uv_cache uv pip install \
  --python .venv/bin/python -e . pytest
```

结果：解析并安装 220 个包；`genkai==2.2.0` 以 editable 方式指向当前工作树。

环境验证：

- Python：`3.12.9`；
- pytest：`9.1.1`；
- ASE：`3.29.0`；
- pandas：`3.0.5`；
- Pydantic：`2.13.5`；
- `genkai.__file__`：当前工作树的 `src/genkai/__init__.py`；
- `.venv/bin/python scripts/check_harness.py ci`：`21 passed in 43.11s`，三个公共
  CLI `--help` 全部通过。

## Agent 日常任务准备

读取本日志后，完成一次不启动在线模型和科学计算的 Agent 日常准备：

- 确认当前分支为 `feat/genkai-evolution`；
- 确认激活解释器为当前工作树 `.venv/bin/python`，版本 `3.12.9`；
- `python scripts/check_harness.py doctor`：通过；
- `python scripts/check_harness.py quick`：`12 passed in 0.32s`；
- `agent run --help`：通过；
- 初始化 `agents/Agent/.workspace/`，生成被 Git 忽略的 workspace `MEMORY.md`；
- 执行 `.venv/bin/agent init .`，将 Agent CLI 的 `project_root` 设置为当前
  `Genkai_Evolution` 工作树。

当前 `agents/Agent/.env` 不存在，因此准备状态只证明本地 Harness、Agent 导入、CLI、
workspace 和项目根目录可用；没有尝试在线 LLM 调用，也没有创建或复制任何凭据。

## 8 月 5 日计划的日常工作指南

完整读取 `work_logs/2026-08-05.md` 后核对当前源码：Task 1 的
`AdsorbateLandscapeConfig`、`AdsorbateLandscapeResult`、
`run_adsorbate_landscape()` 和目标测试仍不存在。

新增 `docs/daily-agent-work-guide.md`，把下一次日常工作限制为 Task 1 的离线 TDD
切片，使用当前 `.venv` 和 Harness profile，明确禁止网络、LLM、Materials Project、
VASP、MLIP、GPU/PJM、训练和 MD。`AGENTS.md` 与 `work_log.md` 已增加该指南入口。

## 日常工作执行：Adsorbate 程序化 API

按照日常指南只执行 8 月 5 日计划的 Task 1，没有开始 structure-only executor 或
workflow 接入。

实际修改：

- `src/genkai/modeling/surface/adsorbate.py`：新增不可变的
  `AdsorbateLandscapeConfig`、`AdsorbateLandscapeResult` 和
  `run_adsorbate_landscape()`；原 `main()` 保留全部 CLI 参数，只负责解析、smoke-test
  覆盖值转换和调用程序化 API。
- `tests/modeling/test_adsorbate_programmatic_api.py`：使用最小 Sn/O slab 与 H2 输入，
  验证确定性生成顺序、两个 ASE 可读 CIF、CSV、图和最佳候选文件。

TDD 证据：

- RED：新测试首先以
  `AttributeError: module ... has no attribute 'run_adsorbate_landscape'` 失败，
  `1 failed, 1 warning`；
- GREEN：最小实现后新测试为 `1 passed, 14 warnings`；
- 目标回归：新测试、surface algorithm ownership 和 paperread compatibility 合计
  `31 passed, 14 warnings, 7 subtests passed in 17.72s`；
- 模块 CLI 与 Skill wrapper 的 `--help` 均通过，参数保持一致。
- Harness CI：`21 passed in 38.90s`，三个公共 CLI `--help` 全部通过；
- wheel/package：首次发现当前 uv 环境缺少 `python -m pip`，安装 `pip==26.2.1`
  后进一步确认缺少 no-build-isolation 所需的 `setuptools.build_meta`；安装
  `setuptools==84.0.0` 和 `wheel==0.48.0` 后，原 package profile 为
  `1 passed in 10.20s`。安装指南已同步补充这三个构建工具。

离线 CLI 检查使用保存的 SnO2 slab 和 H2，显式设置 `calculator=none`、
`max_steps=0`、一个 coverage 和一个 uniform pattern。结果在临时目录生成：

- 1 个 ASE 可读 CIF，122 个原子；
- `adsorbate_coverage_landscape.csv`；
- `adsorbate_coverage_landscape.png`；
- `workflow_test_best_candidate.cif`。

输出中的 `0.02969046 eV` 来自 mock calculator，只能证明工作流和文件输出，不是吸附能
或结构弛豫证据。本轮没有访问网络、在线 LLM、Materials Project，没有运行 UMA、
MACE、DeepMD、VASP、GPU/PJM、训练或 MD，也没有 commit、push 或创建 PR。

## 未运行

- 未运行 Python lint、formatter 或静态类型检查，因为仓库尚未配置这些命令。
- 未运行 `npm run build`，因为没有修改前端文件。
- 未运行 external 测试、网络服务或任何科学计算。
- 未提交 commit、未推送远端、未创建 PR。

## 残余风险

- 完整默认测试仍被 structure builder 的旧测试阻塞。
- 当前 CI 是明确的最低门禁，不包含 wheel profile 和完整默认测试。
- 原有未跟踪文件 `core.genkai0001.4035689` 未被读取、修改或删除。
