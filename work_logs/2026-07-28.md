# 2026-07-28 工作日志

## 验证 MACE、UMA 与 DeepMD 的 MLIP skill 接入

对 `agents/Agent/skills/mlip/` 中 MACE、UMA 和 DeepMD 三个 launcher
进行了路径预检、真实轻量 CPU 测试和输出核对。本次测试均在前台运行，没有提交
PJM 作业；测试目录统一为：

`/tmp/genkai_mlip_smoke_20260728_01/`

### 通用接入检查

- `mlip` skill 能通过 ADK 的 `load_skill_from_dir` 正常加载，并识别
  `submit_mace_calculation.sh`、`submit_uma_calculation.sh` 和
  `submit_deepmd_calculation.sh`。
- 三个 launcher 的 Bash 语法检查通过。
- 三个 launcher 的 dry-run 均通过，正确解析各自的运行时、虚拟环境、工作/输出
  目录、入口和最终命令。

### MACE CPU 单点测试

- 使用 `MACE-MP small`、CPU、1 个线程和 `float32`，对 3 原子 CO2 结构执行
  单点能量/力计算。
- 计算正常结束，能量为 `-22.820255279541016 eV`，最大原子力模为
  `1.068371803667985 eV/Ang`。
- 成功生成：
  - `/tmp/genkai_mlip_smoke_20260728_01/mace/results.json`
  - `/tmp/genkai_mlip_smoke_20260728_01/mace/evaluated.extxyz`
- 受本次受限测试环境影响，共享 Matplotlib 缓存目录不可写，Matplotlib 自动改用
  `/tmp` 缓存；该警告未影响 MACE 模型加载或计算结果。

### UMA CPU 快速推理测试

- 使用本地缓存中的 `uma-s-1p1` 模型和 `omat` 任务头，对 2 原子周期性 Cu
  结构执行 CPU 快速正确性检查。
- 使用 1 个线程、无预热、1 次推理；模型加载耗时约 `20.30 s`，单次推理耗时
  `2.555013 s`，所有检查属性均为有限值，状态为 `PASS`。
- 测试使用 `fairchem-core 2.21.0` 和 `torch 2.8.0+cu128`，保持离线模式，
  没有下载模型。
- 结果写入：
  `/tmp/genkai_mlip_smoke_20260728_01/uma/result.csv`

### DeepMD 单帧模型测试

- 使用 TensorFlow 后端、CPU 和 1 个线程，以已有 `graph.pb` 对教程验证数据执行
  `dp test -n 1`。
- 首次在受限沙箱中启动时，Genkai module 系统因不能访问 `/dev/log` 而停止；
  在允许 module 正常工作的环境中重新运行后测试通过。该问题属于测试沙箱限制，
  不是 DeepMD launcher、运行时或模型故障。
- 1 帧测试得到：
  - Energy MAE：`5.094510e-03 eV`
  - Energy MAE/Natoms：`1.018902e-03 eV`
  - Force MAE：`4.762698e-02 eV/Ang`
  - Virial MAE：`6.626973e-02 eV`
- 成功生成 `smoke.e.out`、`smoke.e_peratom.out`、`smoke.f.out`、
  `smoke.v.out` 和 `smoke.v_peratom.out`，位于：
  `/tmp/genkai_mlip_smoke_20260728_01/deepmd/`

### 验证结论与边界

- MACE、UMA 和 DeepMD 三套软件均已证明可以通过 Genkai_Agent 的 `mlip`
  skill launcher 解析环境并完成真实轻量 CPU 计算，三项接入测试均为 `PASS`。
- 本次没有验证 GPU/CUDA、PJM 提交、长时间结构弛豫、训练、分子动力学或
  DeepMD-LAMMPS；这些仍需在相应计算节点上单独验证。
- 本次只新增工作日志和索引记录，没有修改三个 launcher 或 MLIP skill 的实现。
