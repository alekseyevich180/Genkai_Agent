# 2026-07-07 工作日志

## 从 tests/papers 派生数据扩充 paperread 知识库

本次继续 2026-07-06 的 `paperread` 整合工作，目标是把 `tests/papers` 已经抽取出的批量结果转成可复用知识，而不是重新读取 PDF 或生成单篇论文报告。

使用的数据来源：

- `tests/paperread_batch_experience/*_surface_relations.jsonl`
- `tests/paperread_batch_experience/*_table.csv`

执行方式：

- 读取最新工作日志，确认当前约定是只保留统一的 `paperread` skill。
- 检查后台没有残留的 `collect-experience` 进程，避免并发写入经验库。
- 使用一次性聚合方式读取 37 组 relations/table，避免逐篇运行时反复刷新 registry。
- 将聚合结果写入 canonical material class 经验库：`paperread/surface/experience/material_classes/*.json`。
- 将未知或未映射术语写入 skill 侧经验库：`agents/Agent/skills/paperread/experience/unrecognized_surface_terms.jsonl`。
- 重建参数注册表：`agents/Agent/skills/paperread/experience/surface_parameter_registry.json` 和 `.md`。
- 运行 `.venv/bin/agent knowledge seed`，尝试将当前 skills/guides 重新写入 Know-Do Graph。
- 运行 `.venv/bin/agent knowledge stats` 查看图谱状态。

本次收集结果：

- 处理 relations/table 配对数：37。
- 原始经验项：1166。
- 去重后经验项：1166。
- 聚合后 material class 条目：593。
- 其中 known useful：108。
- 其中 unknown：485。
- 写入或更新的材料类数量：14。
- skill 侧未知术语 JSONL 当前行数：542。

更新后的主要材料类规模：

- `metals_alloys`：455 terms。
- `oxides`：148 terms。
- `supported_catalysts`：86 terms。
- `hydroxides_oxyhydroxides`：61 terms。
- `other_inorganic_materials`：53 terms。
- `nitrides`：50 terms。
- `carbides_mxenes`：44 terms。
- `phosphides_phosphates`：27 terms。
- `single_atom_catalysts`：23 terms。
- `carbon_materials`：20 terms。
- `sulfides`：14 terms。
- `defect_engineered_materials`：11 terms。
- `battery_electrode_materials`：3 terms。
- `selenides_tellurides`：2 terms。

registry 状态：

- `surface_parameter_registry.json` 覆盖 20 个材料类。
- common element keywords 包括 `Pt`, `C`, `O`, `Ru`, `Co`, `Au`, `H`, `Ir`, `Ti`, `N`, `Ag`, `Sb`, `Ni`, `Fe`, `Cr`, `W`, `Pd`, `Rh`, `V`, `Cd`。
- reaction keywords 包括 electrocatalytic, electrocatalysis, Catalytic Oxidation, fuel cells, OER, oxygen evolution reaction, Electrocatalysis 等。

Know-Do Graph 状态：

- `.venv/bin/agent knowledge seed` 执行成功，返回 `Created 0 entries and 0 dependency edges`，说明 skill 节点已经存在，本次主要更新的是 skill experience 与 registry 文件。
- `.venv/bin/agent knowledge stats` 显示 Know-Do graph 有 45 个 nodes、66 条 edges。
- MemGraph 当前有 3 个 entries，3 个 unpromoted memory。

注意事项：

- 本次没有生成新的 per-run JSON/Markdown 长报告，避免把论文来源列表作为主要知识形态。
- 本次知识扩充重点是统计关键词、材料类、元素、组成、状态、修饰、反应和建模 cue。
- 运行 unknown-term 导出时出现 Agent 实验性配置 warning，但输出成功，不影响经验文件。
- 后续需要继续清洗 `unknown` 中的噪声，把明显已知的元素、价态、反应缩写纳入 ontology 或 known token，减少 unknown-term 误报。

## 清洗 skill 侧 unknown-term 规则

在比较 `paperread` skill 侧 unknown-term 与 `material_classes` 经验库后，发现旧规则会把基础元素符号、OER/HER/ORR 等反应缩写、常见表面位点、DFT/表征方法词和 `links/source/cites` 等来源字段写入 unknown-term。这不符合当前目标：unknown-term 应主要保存真正值得后续学习的材料、结构、配位、团簇、覆盖状态和建模 cue。

本次规则调整：

- 在 `paperread/surface/surface_ontology.py` 中补全周期表元素到 Og。
- 新增 `REACTION_ABBREVIATIONS`，覆盖 OER、HER、HOR、ORR、CO2RR、NRR、MOR、EOR、UOR 等常见反应缩写。
- 扩充常见表面术语，包括 terrace、step、kink、edge、basal plane、Miller index、exposed facet、clean surface、subsurface、surface defect、surface reconstruction 等。
- 将 DFT、density functional theory、density of states、Bader charge、XAS、XPS、XRD、SEM、TEM、Raman、FTIR、CV、LSV、EIS 等通用计算/表征词作为已知通用词过滤。
- 新增 `is_known_surface_experience_term()`，统一判断元素符号、元素列表、反应缩写、简单价态、Miller 面和常见表面/建模通用词。
- `agents/Agent/skills/paperread/scripts/export_surface_experience.py` 改为使用统一 known-term 判断。
- unknown-term 导出不再扫描 `links` 字段，避免把 source、cites、DOI 当作 skill 经验。

重新生成结果：

- `unrecognized_surface_terms.jsonl` 从 542 行降到 161 行。
- material class 聚合条目为 662，其中 known useful 266，unknown 396。
- `metals_alloys` 中 known useful 从 69 提升到 148，unknown 降到 302。
- `oxides` 中 known useful 提升到 51，unknown 降到 96。
- `supported_catalysts` 中 known useful 提升到 33，unknown 降到 53。

验证样例：

- `Co`、`Pt`、`Au`、`{Ag, Ce, O, Ni}` 被识别为已知元素或元素列表。
- `OER`、`HER`、`ORR`、`CO2RR` 被识别为已知反应缩写。
- `surface`、`top site`、`bridge site` 被识别为常见表面术语。
- `DFT`、`density of states`、`Bader charge analysis`、`XAS` 被识别为通用计算/表征词。
- `Fe-N4` 仍保留为可学习词，因为它表示具体配位环境，后续可用于 single atom / carbon material 建模。

后续仍需清理的内容：

- `PEC`、`photoelectrochemical`、`photoanode` 是否应作为通用应用词过滤，需要根据后续是否处理光电催化建模决定。
- `Au(111)`、`Au(100)` 这类材料+晶面组合目前仍保留为可学习词，因为它们可能直接对应 slab 建模输入。
- `AgSA`、`single-atom Au`、`High OH or mixed OH/O` 这类词应继续保留，属于后续建模参数或局部结构描述。

## 处理 tests/papers2 的论文抽取

本次开始处理 `tests/papers2` 中的第二批 PDF，共 21 篇。目标仍然是通过 `paperread` 统一入口抽取论文信息，再把可复用关键词和经验合并进知识库。

处理过程：

- 检查 `tests/papers2`，确认共有 21 个 PDF。
- 初次运行 `paperread_tools.py surface-pipeline` 时因沙箱网络限制失败，错误为 API connection error。
- 使用联网权限重跑后，确认第一篇可以成功抽取，但后续连续调用触发 `RateLimitError`。
- 发现未使用 `--keep-intermediate` 时 PDF 输出会使用临时 stem `surface_conditions_input`，多篇 PDF 会互相覆盖。
- 给 `paperread/surface/common.py` 的 `chat_completion()` 增加有限重试和退避，用于处理 rate limit、connect、timeout 等临时错误。
- 改用 `--keep-intermediate` 重新运行，确保每篇 PDF 生成独立的 text、sections、conditions_input、relations_input。
- 由于 API 仍处于限流状态，批处理停止，避免继续浪费请求。

当前已生成的可用输出：

- 1 组完整 relations/table 输出：`tests/paperread_papers2_experience/surface_conditions_input_surface_relations.jsonl` 和 `surface_conditions_input_table.csv`。
- 1 个 table-only 输出：`Advanced Energy Materials - 2020 - Luo - Interface Engineering of Hierarchical Branched Mo‐Doped Ni3S2 NixPy Hollow_conditions_input_table.csv`。
- 若干 keep-intermediate 中间文件，包含 PDF 文本、sections、conditions_input、relations_input，可供后续断点续跑。

已合并进知识库的内容：

- 将完整 relations/table 和 table-only 输出合并进 `paperread/surface/experience/material_classes/*.json`。
- 本次新增/合并 44 个原始经验项，聚合为 29 个 material class 条目。
- 涉及材料类：`carbon_materials`、`defect_engineered_materials`、`metals_alloys`、`oxides`、`supported_catalysts`。
- 从完整输出导出 8 条 skill 侧 unknown-term 到 `agents/Agent/skills/paperread/experience/unrecognized_surface_terms.jsonl`。
- 重建 `agents/Agent/skills/paperread/experience/surface_parameter_registry.json` 和 `.md`。

当前相关材料类统计：

- `carbon_materials`：20 terms，known useful 9，unknown 11。
- `defect_engineered_materials`：11 terms，known useful 11，unknown 0。
- `metals_alloys`：458 terms，known useful 150，unknown 308。
- `oxides`：160 terms，known useful 53，unknown 107。
- `supported_catalysts`：92 terms，known useful 35，unknown 57。
- skill 侧 `unrecognized_surface_terms.jsonl` 当前 169 行。

遗留问题：

- `tests/papers2` 的多数 PDF 还没有完成 relation/table 全流程抽取，主要阻塞是 API rate limit。
- 后续应基于 `tests/paperread_papers2_experience/*_conditions_input.json` 和 `*_relations_input.json` 做断点续跑，而不是重新解析 PDF。
- 继续处理时必须启用 `--keep-intermediate` 或直接使用中间 JSON，避免不同 PDF 输出互相覆盖。
- 建议降低批处理频率，或者分批运行 2-3 篇后等待一段时间再继续。

## 继续处理 tests/papers2：断点与无 API fallback

继续处理 `tests/papers2` 时，先做断点检查：

- 第 1 篇已有 keep-intermediate 中间 JSON，但缺 table 和 relations。
- 第 2 篇已有 table，但缺 relations。
- 其余 PDF 起初没有中间文件。

尝试对第 2 篇单独补 `extract_surface_relations`，但 API 返回 `AuthenticationError: Unauthorized`。这说明当前不能继续依赖 LLM API 完成正式 conditions / relations 抽取。

为避免停在 API 问题上，本次改为无 API fallback：

- 用 `paperread.surface.ingest_pdf.ingest_pdf()` 为 21 篇 PDF 全部生成中间文件。
- 输出目录仍为 `tests/paperread_papers2_experience/`。
- 所有 PDF 均已生成 `*_text.txt`、`*_sections.json`、`*_conditions_input.json`、`*_relations_input.json`。
- 基于本地文本和 `surface_ontology.py` 做严格启发式扫描，抽取元素/公式、材料类、晶面、反应缩写/反应名、表面/支撑、负载、掺杂/界面、单原子/团簇、吸附物/反应物。
- 不再收集 DFT、DOS、Bader、XAS、links/source/cites 等通用方法或来源字段作为 unknown。

为清除第一次宽松启发式带来的噪声，本次从头重建 canonical material class：

- 合并 `tests/paperread_batch_experience` 的正式 relations/table。
- 合并 `tests/paperread_papers2_experience` 当前已有的正式输出。
- 合并 `papers2` 的严格本地启发式文本扫描结果。
- 覆盖 `paperread/surface/experience/material_classes/*.json`。
- 重建 `agents/Agent/skills/paperread/experience/surface_parameter_registry.json` 和 `.md`。
- 重新生成 skill 侧 `unrecognized_surface_terms.jsonl`，只使用正式 relations/table 输出，不使用启发式文本结果。

最终状态：

- 全部 21 篇 PDF 已完成无 API 中间文件生成。
- material class 重建输入项：2401。
- material class 聚合条目：972。
- known useful：363。
- unknown：609。
- skill 侧 unknown-term：173 条。

主要材料类当前规模：

- `metals_alloys`：501 terms，known useful 156，unknown 345。
- `oxides`：172 terms，known useful 54，unknown 118。
- `supported_catalysts`：94 terms，known useful 37，unknown 57。
- `hydroxides_oxyhydroxides`：65 terms，known useful 20，unknown 45。
- `nitrides`：53 terms，known useful 32，unknown 21。
- `carbides_mxenes`：45 terms，known useful 15，unknown 30。
- `carbon_materials`：21 terms，known useful 9，unknown 12。
- `defect_engineered_materials`：20 terms，known useful 20，unknown 0。

注意：

- 当前 papers2 的 fallback 结果不是 LLM 级关系抽取，只能作为关键词级知识扩充。
- 后续 API 恢复后，应基于 `tests/paperread_papers2_experience/*_conditions_input.json` 和 `*_relations_input.json` 继续补正式 table/relations，而不是重新解析 PDF。
- `paperread/surface/common.py` 已加入 API 临时错误重试，但认证错误无法通过重试解决，需要外部 API 配置恢复。

## unknown-term 再统计并写入 skill 经验

根据人工检查，unknown 数据中仍有不少元素英文名、材料类别英文标签、化学式、组成名和反应应用词。继续扩展 `surface_ontology.py` 的 known 判断后，重新构建 material class 与 skill unknown 统计。

已纳入 known 判断的类别：

- 元素英文名称，例如 silver、bismuth、nickel、oxygen。
- 材料类别标签，例如 `metals_alloys`、`oxides`、`hydroxides_oxyhydroxides`。
- 常见分子和化学式，例如 `O2`、`H2O`、`CO2`、`TiO2`、`ZrO2`、`CeO2`。
- 常见反应和应用词，例如 electrocatalysis、oxygen evolution reaction、oxygen reduction reaction、water splitting、fuel cells、ketonization。

重建后的统计：

- material-class terms：1098。
- known useful：634。
- unknown entries：464。
- unknown occurrence count：595。
- skill 侧 `unrecognized_surface_terms.jsonl`：129 条。

unknown 分类统计：

- `other_unknown`：366。
- `composite_material_name`：100。
- `formula_or_composition`：49。
- `structure_or_material_phrase`：44。
- `reaction_or_application`：25。
- `facet_or_miller_index`：10。
- `element_english_name`：1。

本次写入 skill 经验库：

- `agents/Agent/skills/paperread/experience/unknown_term_statistics_2026_07_07.json`
- `agents/Agent/skills/paperread/experience/unknown_term_statistics_2026_07_07.md`

结论：

- 元素英文名和材料类标签基本已经从真正 unknown 中清理出去。
- 剩余 unknown 主要是复合材料名、局部结构名、组成表达、特殊晶面和少量噪声。
- `composite_material_name`、`formula_or_composition`、`structure_or_material_phrase` 多数应该保留为高价值建模词。
- `other_unknown` 仍需要后续人工或规则清洗，其中混有 `Full`、`electronic structure` 等噪声，也有 `Anatase TiO2`、`Pt(111)`、`*OOH`、`carbon doping` 等有用词。
