# 2026-08-03 工作日志

## 目标与范围

在 `Genkai_Evolution/` 专用 worktree 中按方案 A 完成结构优化第二步的
Task 10–11：建立结构基线和依赖门禁，并将 surface literature 业务完整迁入
`src/genkai/`。本 worktree 不保留旧 `paperread.surface.*` import 或
`python -m paperread.surface` CLI。PToModel 和 surface modeling 算法属于
Task 12，本轮不迁移。

## 完成内容

- 建立 `docs/structure-baseline.md` 和 AST import boundary 测试。
- 新增 `src/genkai/llm.py`，统一 LLM 配置并保持客户端延迟初始化。
- 将 surface core、extraction、experience、pipeline 迁入
  `src/genkai/literature/surface/`。
- 新增纯 literature pipeline 和 `genkai.workflows.surface_paper` 组合层；
  `genkai-workflow surface` 提供 9 个子命令。
- 删除旧 surface literature package、module CLI 和示例目录，仅暂留
  `paperread/surface/modeling/` 作为 Task 12 技术债。
- wheel 显式打包全部 20 个 canonical material-class JSON 文件。
- 修复收尾扫描发现的 3 个 Agent paperread skill 旧导入，并增加 4 个实际
  脚本入口测试；默认经验库初始化路径改为新目录。
- 更新 README、长期计划、skill 说明和 registry 来源元数据。

## 提交检查点

- `ce09765` `docs: design surface literature structure convergence`
- `9837c19` `docs: plan surface literature convergence`
- `b26fc74` `test: establish genkai dependency boundaries`
- `ad9ef5d` `refactor: move llm configuration into genkai`
- `fbf1352` `refactor: move surface literature into genkai`
- `b1020a9` `feat: route surface literature through genkai workflow`
- `57ffa96` `refactor: remove legacy surface entrypoints`
- `b36252e` `fix: update paperread skill imports`

## 验证记录

所有命令都使用共享 `.venv`，并在本 worktree 前台执行。

1. 科学输出相关离线回归：

   ```bash
   ../.venv/bin/python -m pytest tests/test_paperread_surface.py tests/integrations/test_surface_facades.py tests/workflow/test_surface_paper.py tests/workflow/test_paper_to_mlip.py -v --tb=short
   ```

   结果：`36 passed, 7 subtests passed`。

2. 架构、LLM 与 wheel：

   ```bash
   ../.venv/bin/python -m pytest tests/architecture tests/literature tests/packaging/test_wheel_contents.py -v --tb=short
   ```

   结果：`9 passed, 4 warnings`。wheel 源文件与安装包均包含同一组 20 个
   material-class JSON 文件。

3. CLI 无密钥加载：

   ```bash
   ../.venv/bin/python -m genkai.cli --help
   ../.venv/bin/python -m genkai.cli surface --help
   ../.venv/bin/python -m genkai.cli surface list-tools
   ```

   结果：三个命令均以状态码 0 退出，不需要 LLM API key。

4. Agent paperread skill 入口：

   ```bash
   ../.venv/bin/python -m pytest tests/skills/test_paperread_entrypoints.py -v --tb=short
   ```

   结果：`4 passed`。测试覆盖三个脚本的真实 `--help` 调用和新布局下的
   material-class 初始化。

5. 完整相关回归：

   ```bash
   ../.venv/bin/python -m pytest tests/contracts tests/workflow tests/integrations tests/skills tests/test_paperread_surface.py tests/test_surface_mp_workflow.py tests/test_agent.py -v --tb=short
   ```

   结果：`107 passed, 42 warnings, 7 subtests passed`。警告来自既有
   `BaseAgentConfig`、ASE/NumPy 和 spglib deprecation。

6. 仓库级收集检查：

   ```bash
   ../.venv/bin/python -m pytest -q --tb=short
   ```

   结果：在执行测试前停止，`tests/test_structure_builder.py` 收集时出现
   `ModuleNotFoundError: No module named 'agent.tools'`，共 `1 error`。这是
   Task 1–9 阶段已记录的既有仓库边界，本轮没有修改或绕过该缺失模块；因此
   不能声称仓库全量测试通过。

## 发现并修复的问题

初次完整回归为 `103 passed, 42 warnings, 7 subtests passed`，但一致性扫描
发现三个 paperread skill 脚本仍导入已删除的旧 surface literature 模块。
实际运行 `paperread_tools.py --help` 可稳定复现 `ModuleNotFoundError`。新增失败
测试后，将入口改为 `genkai.literature.surface`，最终相关回归增加到 107 项并
全部通过。

## 明确未执行

本轮没有运行在线 LLM 抽取、VASP、GPU/CUDA、PJM、结构弛豫、MACE 科学
推理、DeepMD 训练、UMA 微调或分子动力学。所有抽取验证均为 mock、保存的
fixture 或离线 replay；这些结果不是当前在线抽取或真实科研计算证据。

## 剩余边界

Task 12 仍需消除以下两条精确反向依赖，allowlist 不得扩大：

- `src/genkai/modeling/ptomodel.py` ->
  `paperread.surface.modeling.job_bundle.build_modeling_checklist`
- `src/genkai/modeling/ptomodel.py` ->
  `paperread.surface.modeling.ptomodel.build_ptomodel_payload`

## 后续会话交接：Task 12–15 范围待定

Task 10–11 完成后，用户要求“全部执行”。由于该表述可能分别指向代码结构
收敛、既有全量测试阻断修复，或真实外部科研运行和 Git 发布，本次没有扩大
授权范围，也没有开始新的设计或代码修改。已向用户给出以下一次性范围选择：

- A：完成 Task 12–15 的全部代码结构收敛；
- B：在 A 的基础上修复既有 `agent.tools` 全量测试收集阻断；
- C：在 B 的基础上还包括在线 LLM、VASP/GPU/PJM、训练任务和远端推送。

用户随后要求先把进程写入日志，因此当前暂停在范围选择之前；尚未收到 A、B
或 C。下次会话不得从“全部执行”自动推断 C，也不应在没有范围选择时启动外部
任务或推送。

恢复顺序：

1. 在 `/home/pj24001724/ku40000345/wu/Genkai_Agent/Genkai_Evolution`
   检查 `feat/genkai-evolution` 工作树和提交状态；写入本交接前工作树干净，
   `HEAD=63846da`，相对 `origin/feat/genkai-evolution` 本地领先 10 个提交且
   尚未推送。
2. 读取 `GENKAI_EVOLUTION_PLAN.md` 第 15.5–15.6 节、本日志和已完成的
   Task 10–11 设计/实施计划。
3. 获取或确认 A/B/C 范围；这是剩余阶段唯一必要的范围门禁，不需要恢复逐步
   确认。
4. 若选择 A 或 B，先为 Task 12–15 分阶段编写设计和逐文件计划，再采用测试
   先行连续执行。Task 12 应从上节两条精确 allowlist 开始，且不得扩大反向
   依赖。
5. 保持 Skills 作为按需决策、外部入口和实验孵化层，稳定复用逻辑继续收敛到
   `src/genkai/`；不要把新 facade 本身描述为已经完成物理目录收敛。
