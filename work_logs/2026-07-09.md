# 2026-07-09 工作日志

## 解决 unknown data 中的晶面指数换算问题

本次接续 2026-07-08 unknown-term 清洗结果，优先处理 `facet_or_miller_index` 类 unknown。核心问题是晶面指数不能只做字符串归一化，必须结合材料的晶体结构、space group 和软件切表面的三指数输入约定。

已完成：

- 新增 `paperread/surface/surface_indices.py`
  - 解析普通 Miller 三指数与 hexagonal Miller-Bravais 四指数。
  - 支持 Unicode overbar，例如 `(101̅4)`、`(0112̅)`。
  - 将 hexagonal/hcp 四指数 `hkil` 转成软件常用三指数 `(hkl)`。
  - 内置首批结构上下文：
    - `Ru`: hcp, `P6_3/mmc (No. 194)`, `Ru(0001)` -> `(001)`。
    - `ZnO`: wurtzite, `P6_3mc (No. 186)`, `ZnO(10-10)` -> `(100)`。
    - `β-CoOOH`: layered hexagonal/trigonal context, `β-CoOOH(0112̅)` 按常见抽取错位修正为 `(01-12)`，软件输入 `(012)`，并保留 warning。
- 更新 `paperread/surface/ptomodel.py`
  - `normalized_mapping.facet_set` 现在输出软件可用三指数。
  - `selected_information.surface_facets[]` 增加 `surface_index` 结构化字段，保留原始指数、输入 notation、软件 Miller index、space group/structure 提示和 warnings。
- 更新 `paperread/surface/collect_experience.py`
  - `exposed_surfaces` 能识别带 overbar 的四指数。
  - class profile 增加 `surface_index_mappings`。
  - 对孤立的 `(0112̅)` 这类 facet，会从同一 profile 的材料词中带入 `β-CoOOH`、`ZnO`、`Ru` 上下文后再换算。
- 更新 `paperread/surface/parameter_registry.py`
  - registry JSON/Markdown 增加 surface index mappings。
  - prompt hint 会回灌 `facet mappings`，便于后续抽取和 PToModel 使用。
- 更新 `paperread/surface/unknown_terms.py`
  - `classify_unknown_term` 改用结构化 surface index parser。
  - `(101̅4)` 等四指数不再落入 `other_unknown`。
  - 修复 material-class store 刷新条件，使 profile 字段变化也能写回。

重建结果：

- `agents/Agent/skills/paperread/experience/surface_parameter_registry.{json,md}` 已重建。
- `agents/Agent/skills/paperread/experience/unknown_term_statistics_2026_07_09.{json,md}` 已生成。
- 2026-07-09 unknown category 变化：
  - `facet_or_miller_index`: 40 -> 46。
  - `other_unknown`: 240 -> 234。
- 抽查结果：
  - `Ru(0001)` -> software facet `(001)`。
  - `β-CoOOH(0112̅)` -> corrected hkil `[0, 1, -1, 2]` -> software facet `(012)`。
  - `ZnO(10-10)` -> software facet `(100)`。

验证：

- `python -m py_compile paperread/surface/surface_indices.py paperread/surface/collect_experience.py paperread/surface/parameter_registry.py paperread/surface/ptomodel.py paperread/surface/unknown_terms.py` 通过。
- `PYTHONPATH=. python tests/test_paperread_surface.py`：14 tests passed。

遗留问题：

- `β-CoOOH` 的 space group 仍应在真正建模前以具体 CIF 为准；当前 registry 只给结构上下文和换算提示，不替代 CIF 级结构确认。
- 下一步可以继续处理 `facet_or_miller_index` 之外的 unknown，如 adsorbate/intermediate、coverage、active-site coordination 和 composite material names。

## 清理已映射面指数的 unknown 残留

根据复查，`unknown_term_statistics_2026_07_09` 中仍保留了 `facet_or_miller_index` 类别，且 `unrecognized_surface_terms` 中还有 `Au(111)`、`Au(100)`。这些 term 已经可以由 `surface_indices` 解析并进入 `surface_index_mappings`，因此不应继续作为 unknown。

追加修改：

- `paperread/surface/unknown_terms.py`
  - `reclassify_material_class_store` 对 `canonicalize_surface_index(...)` 成功的 term 直接提升为 `known_useful`。
  - 这类 term 的 `research_category` 设为 `surface_structure`。
  - 对孤立 facet 会使用同一 material-class profile 中的材料上下文辅助判断。
- `agents/Agent/skills/paperread/scripts/export_surface_experience.py`
  - 导出 unknown/unrecognized term 时，将 `is_surface_index_term(...)` 成功的 term 视为已识别，不再写入 `unrecognized_surface_terms`。
- `paperread/surface/surface_indices.py`
  - 兼容 `Pt(111`、`β-CoOOH(0112̅` 这类缺右括号的抽取残片。
- `tests/test_paperread_surface.py`
  - 增加缺右括号面指数解析回归检查。

重建结果：

- material-class store 中 29 个面指数相关 term 被提升为 known。
- `unknown_term_statistics_2026_07_09`：
  - known useful: 697 -> 726。
  - unknown entries: 401 -> 372。
  - unknown occurrence count: 478 -> 432。
  - `facet_or_miller_index` 类别已消失。
- `unrecognized_surface_terms.jsonl/md`：
  - 删除 `Au(111)`、`Au(100)` 两条已识别面指数 term。
  - 107 records -> 105 records。

验证：

- `PYTHONPATH=. python tests/test_paperread_surface.py`：14 tests passed。
- 脚本检查确认：
  - `unknown_term_statistics_2026_07_09` 的 top unknown 中没有可由 `surface_indices` 解析的 term。
  - `unrecognized_surface_terms.jsonl` 中没有可由 `surface_indices` 解析的 term。

## 增加矿物/晶体结构原型词典

根据进一步检查，`rutile`、`spinel`、`anatase`、`perovskite` 等词不应作为普通 material unknown 处理。这类矿物名/结构原型名往往直接提示晶体结构、space group family 和可用的 facet notation。

追加修改：

- 新增 `paperread/surface/crystal_structures.py`
  - 集中维护晶体结构/矿物原型词典。
  - 首批包含：`rutile`、`anatase`、`brookite`、`spinel`、`normal spinel`、`inverse spinel`、`perovskite`、`fluorite`、`wurtzite`、`rocksalt`、`rock salt`、`pyrochlore`、`delafossite`、`brucite`、`corundum`。
  - 每个词条记录 family、crystal system、typical space group 和必要说明。
- 更新 `paperread/surface/surface_ontology.py`
  - `is_known_surface_experience_term` 能识别结构原型词。
- 更新 `paperread/surface/collect_experience.py`
  - class profile 新增 `crystal_structure_terms`。
  - `_detect_spacegroup_terms` 改为复用结构词典，不再靠零散字符串。
- 更新 `paperread/surface/parameter_registry.py`
  - registry common 增加 `crystal_structure_keywords`。
  - Markdown/prompt hint 增加 crystal/mineral structure keywords。
- 更新 `paperread/surface/unknown_terms.py`
  - reclassify 时将结构原型词提升为 `known_useful`，category 为 `surface_structure`。
- 更新 `agents/Agent/skills/paperread/SKILL.md`
  - 明确要求将 rutile/spinel 等矿物/结构原型名作为 crystal-structure cue 处理。

重建结果：

- 12 个结构相关 unknown term 被提升为 known。
- `unknown_term_statistics_2026_07_09`：
  - known useful: 726 -> 738。
  - unknown entries: 372 -> 360。
  - unknown occurrence count: 432 -> 413。
- `surface_parameter_registry.md` 顶部新增：
  - `Crystal structure keywords: rutile, spinel, normal spinel, perovskite, anatase`
- metals/alloys 和 oxides profile 中已经显示：
  - rutile -> tetragonal。
  - anatase -> tetragonal。
  - spinel / normal spinel -> cubic。

验证：

- `python -m py_compile paperread/surface/crystal_structures.py paperread/surface/surface_ontology.py paperread/surface/collect_experience.py paperread/surface/parameter_registry.py paperread/surface/unknown_terms.py agents/Agent/skills/paperread/scripts/export_surface_experience.py` 通过。
- `PYTHONPATH=. python tests/test_paperread_surface.py`：14 tests passed。
- 脚本检查确认 top unknown 和 `unrecognized_surface_terms` 中均无可由 crystal-structure dictionary 识别的 term。

## 清理 composite_material_name 与 formula_or_composition unknown

本轮按要求处理 `composite_material_name` 和 `formula_or_composition` 两类 unknown。处理方式是先用网页检索核对代表性材料/组分对象，再写入可复用词典，而不是只在统计文件里手工删项。

网页核对依据：

- `g-C3N4`：graphitic carbon nitride / graphitic carbon nitride family。
- `LDH`：layered double hydroxides，层状双氢氧化物材料类。
- `Ba0.5Sr0.5Co0.8Fe0.2O3-d`：BSCF perovskite oxide OER catalyst。
- `black phosphorus`：稳定磷同素异形体，层状/二维材料。
- `NiFe LDH`：nickel-iron layered double hydroxide OER catalyst。
- `*OH`、`*O`、`*OOH`、`H*` 等：电催化 OER/HER/ORR 中吸附中间体写法，`*` 表示表面吸附位。
- `polyaniline fibers`：polyaniline conducting polymer fiber/nanofiber material。
- `ultrananocrystalline diamond`：UNCD carbon/diamond material。

追加修改：

- 新增 `paperread/surface/material_vocabulary.py`
  - 集中管理 composite material、variable composition、support shorthand、adsorbed-intermediate notation、electrolyte concentration、surface-site shorthand。
  - 首批覆盖高频项：
    - `β-CoOOH`、`g-C3N4`、`Ag/g-C3N4`、`Co–Fe–Cr (oxy)Hydroxides`、`Co oxides`。
    - `Mo-Ni3S2/NixPy/NF electrode`、`NixPy`、`Ni1−xFexOOH`。
    - `NiFe LDH`、`NiCo LDH/CC`、`sAu/NiFe LDH`、`Sn SAs/G-Na`。
    - `Ba0.5Sr0.5Co0.8Fe0.2O3–d`、`black phosphorus`、`carbon cloth`、`graphene-like carbon`。
    - `*OOH`、`*OH`、`*O`、`H*`、`Hads`、`Habs`、`OHads`、`HO*`、`HOO*`、`NO2*`、`ONOO*`、`*H2O2`、`*O2`。
    - `Cr3c`、`Cr5c`、`1 M KOH`、`1 M NaOH`、Pt-Bi/PtBi alloy terms、polyaniline fibers、ultrananocrystalline diamond。
- 更新 `paperread/surface/surface_ontology.py`
  - material vocabulary term 视为 known surface experience term。
  - `Synthesis` 等 generic reaction type 不再误入 formula unknown。
- 更新 `paperread/surface/unknown_terms.py`
  - reclassify 时使用 material vocabulary，并按词条类型写入 `surface_materials`、`adsorption_reaction`、`clusters_single_atoms`、`surface_structure` 等 research category。
- 更新 `agents/Agent/skills/paperread/scripts/export_surface_experience.py`
  - `unrecognized_surface_terms` 导出时过滤已由 material vocabulary 识别的 term。
- 更新 `agents/Agent/skills/paperread/SKILL.md`
  - 说明 composite material、variable composition、adsorbed intermediate formulae 应先走 material vocabulary。

重建结果：

- material-class store 中 126 个材料/组分相关 term 被提升为 known。
  - 第一轮新增词典后提升 102 个。
  - 补齐剩余项后再提升 24 个。
- `unknown_term_statistics_2026_07_09`：
  - known useful: 738 -> 864。
  - unknown entries: 360 -> 234。
  - unknown occurrence count: 413 -> 248。
  - `composite_material_name` 类别消失。
  - `formula_or_composition` 类别消失。
- `unrecognized_surface_terms.jsonl/md`：
  - 删除 14 条已由 material vocabulary 识别的历史记录。
  - 105 records -> 91 records。

验证：

- `python -m py_compile paperread/surface/material_vocabulary.py paperread/surface/surface_ontology.py paperread/surface/unknown_terms.py agents/Agent/skills/paperread/scripts/export_surface_experience.py` 通过。
- `PYTHONPATH=. python tests/test_paperread_surface.py`：14 tests passed。
- 脚本检查确认：
  - top unknown 中没有可由 material vocabulary 识别的 term。
  - `unrecognized_surface_terms.jsonl` 中没有可由 material vocabulary、surface index 或 crystal-structure dictionary 识别的 term。

## 继续压缩 unknown 到仅剩内部真未知

在前一轮 material / structure 词典基础上，继续补齐了几类高频但被误记为 unknown 的条目：

- 结构/材料碎片：
  - `bimetallic alloy`
  - `phosphides`
  - `tellurides`
  - `Zn-Air` / `Zn-Air Batteries`
  - `single nickel atoms`
  - `octahedral Co`
  - `B-layer (octahedral Co and O ions)`
- 反应/应用通用词：
  - `Shape-dependent electrocatalysis`
  - `Catalytic Oxidation`
  - `(photo)electrocatalysts`
  - `electrocatalytic oxygen evolution`
  - `NO oxidation`
- 电化学/结构描述碎片：
  - `five-coordinated Cr5c`
  - `three-coordinated Cr3c`
  - `monochain CrO3`
  - `OER intermediates`
  - `Nernst equation`
  - `tensile strain`
  - `oxygen evolution`
  - `ΔG*H`
  - `carbon doping`
  - `overpotential` 的表格截断碎片

补充后重新重分类与过滤，得到最终结果：

- material-class store：
  - `known_useful`: 1038
  - `unknown`: 60
- `unknown_term_statistics_2026_07_09`：
  - `terms`: 1098
  - `known_useful`: 1038
  - `unknown`: 60
  - `unknown_categories_by_occurrence`: 仍全部落在 `other_unknown`
- `agents/Agent/skills/paperread/experience/unrecognized_surface_terms.{jsonl,md}`：
  - 已清空，当前没有需要继续手工清理的 skill 侧 unknown

验证：

- `python3 -m py_compile paperread/surface/material_vocabulary.py paperread/surface/surface_ontology.py paperread/surface/collect_experience.py paperread/surface/parameter_registry.py paperread/surface/unknown_terms.py agents/Agent/skills/paperread/scripts/export_surface_experience.py` 通过。
- `PYTHONPATH=. python3 tests/test_paperread_surface.py`：14 tests passed。

## 增加吸附位点与表面/晶面的相关性

根据进一步整理，当前抽取链路里虽然已经能分别识别 `facets`、`active_sites` 和
`adsorption_sites`，但它们还是并列字段，缺少对同一表面上的位点语义关联。为此补了
一层结构化关联：

- `collect_experience.py`
  - 新增 `surface_site_contexts`，把 `surface_terms`、`facet_terms`、`active_site_terms`、
    `adsorption_site_terms` 和 `site_role_terms` 放到同一个上下文里。
  - metal / supported / single-atom 类材料会优先把位点关系解释成 metal-surface site
    geometry，oxides / perovskites / generic materials 采用 surface active-site correlation。
- `ptomodel.py`
  - `normalized_mapping.surface_site_contexts` 现在会直接输出这类关联。
  - `selected_information` 也保留了 `surface_site_contexts`，方便后续任务映射和人工复核。
- `parameter_registry.py`
  - registry 增加 `surface_site_keywords` 和 per-class 的 `top_surface_site_contexts`。
  - prompt hint 会回灌 surface-facet-active-site / adsorption-site 的关联，而不是只回灌独立关键词。
- `summarize_surface_outputs.py`
  - 人类可读 summary 新增“表面-位点关联”小节。
- `SKILL.md` / `README.md`
  - 补充说明：金属表面优先识别 top/bridge/hollow 这类位点语义；氧化物更关注 facet + active site 的耦合。

验证：

- `python3 -m py_compile paperread/surface/collect_experience.py paperread/surface/parameter_registry.py paperread/surface/ptomodel.py paperread/surface/summarize_surface_outputs.py tests/test_paperread_surface.py` 通过。
- `PYTHONPATH=. python3 agents/Agent/skills/paperread/scripts/paperread_tools.py build-parameter-registry` 已重建。
- `PYTHONPATH=. python3 tests/test_paperread_surface.py`：14 tests passed。
